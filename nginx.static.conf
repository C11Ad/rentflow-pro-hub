# Minimal nginx config for SPA on port 3000
server {
    listen 3000;
    listen [::]:3000;
    server_name _;
    
    root /usr/share/nginx/html;
    index index.html;
    
    # SPA fallback - critical for React Router
    location / {
        try_files $uri $uri/ /index.html;
    }
}
